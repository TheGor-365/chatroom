<p style="color: green"><%= notice %></p>

<h1 class="text-center mb-5">
  Rooms
</h1>

  <% @rooms.each do |room| %>
    <%= link_to room.name.capitalize, room, class: "text-success" %>
    <br>
  <% end %>

<h6 class="shadow p-3 rounded text-center my-5 bg-light">
  <%= link_to "Create Chatroom", new_room_path, class: "text-success" %>
</h6>
